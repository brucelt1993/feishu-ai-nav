services:
  frontend:
    build:
      context: ./frontend
      args:
        - VITE_VCONSOLE=${VITE_VCONSOLE:-false}
        - VITE_ALLOW_ANONYMOUS=${VITE_ALLOW_ANONYMOUS:-false}
    ports:
      - "3008:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - DATABASE_URL=postgresql://root:krmbe4bb@host.docker.internal:5432/ai_nav
      - FEISHU_APP_ID=cli_a9db57a7b9f8dbc2
      - FEISHU_APP_SECRET=RE3WK1ivuXU4ddCBWsYcJfkypeECL38G
      - ADMIN_OPEN_IDS=${ADMIN_OPEN_IDS:-}
      - PUSH_CHAT_ID=${PUSH_CHAT_ID:-}
      - DEBUG=true
